<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Cache directory for temporary camera photos -->
    <cache-path
        name="patient_photos"
        path="." />
    
    <!-- External cache directory (backup) -->
    <external-cache-path
        name="external_patient_photos"
        path="." />
</paths>
